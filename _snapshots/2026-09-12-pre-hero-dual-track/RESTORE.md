# Restore: pre hero dual-track
Phrase: `pre-hero-dual-track` · also `quiet authority baseline'a dön`
File-scoped: copy LandingHero.tsx + landing.ts from this folder (do not full restore-snapshot.js).
